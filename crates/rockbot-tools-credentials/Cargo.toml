[package]
name = "rockbot-tools-credentials"
version = "0.1.0"
edition = "2021"
description = "Credential vault access tool for RockBot agents"
license = "MIT"

[dependencies]
rockbot-tools = { path = "../rockbot-tools" }
rockbot-credentials-schema = { path = "../rockbot-credentials-schema" }
rockbot-security = { path = "../rockbot-security" }

serde = { workspace = true }
serde_json = { workspace = true }
