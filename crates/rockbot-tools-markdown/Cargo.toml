[package]
name = "rockbot-tools-markdown"
version = "0.1.0"
edition = "2021"
description = "Markdown/documentation processing tool for RockBot"
license = "MIT"

[dependencies]
rockbot-tools = { path = "../rockbot-tools" }
rockbot-credentials-schema = { path = "../rockbot-credentials-schema" }
rockbot-security = { path = "../rockbot-security" }

serde = { workspace = true }
serde_json = { workspace = true }
