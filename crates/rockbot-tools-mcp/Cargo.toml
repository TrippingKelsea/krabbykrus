[package]
name = "rockbot-tools-mcp"
version = "0.1.2"
edition = "2021"
description = "MCP server connection tool for RockBot"
license = "MIT"

[lints]
workspace = true

[dependencies]
rockbot-tools = { path = "../rockbot-tools" }
rockbot-credentials-schema = { path = "../rockbot-credentials-schema" }
rockbot-security = { path = "../rockbot-security" }

serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
